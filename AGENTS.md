# AGENTS.md

Codex agents should read `CLAUDE.md` for repository guidance. `CLAUDE.md` is the authoritative agent guidance file; this pointer exists to avoid a byte-for-byte duplicate drifting stale.
